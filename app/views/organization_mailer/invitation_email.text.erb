has invite you to join @<%= @membership.organization.account.path %> organization. Head over to <%= account_root_url(@membership.organization.account) %> to checkout @<%= @membership.organization.account.path %>'s profile.

This invitation will expire in 7 days.

Join @<%= @membership.organization.account.path %>: <%= account_invitation_url(@membership.organization.account, invitation_token: @membership.invitation_token) %>
