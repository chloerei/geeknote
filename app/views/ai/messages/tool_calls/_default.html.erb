<%# locals: (tool_call:) %>
<%# RubyLLM::ToolCall; rendered as the "input" section of a tool-call collapse card %>
<pre class="rounded-lg bg-base-100/70 px-2.5 py-2 font-mono text-xs whitespace-pre-wrap break-all"><%= JSON.pretty_generate(tool_call.arguments) %></pre>
