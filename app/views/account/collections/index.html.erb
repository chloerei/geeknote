<% @page_title = @account.display_name %>

<%= render 'account/header' %>

<div class="container container--medium">
  <div class="padding-3 display-flex flex-direction-column gap-3">
    <%= render partial: 'collection', collection: @collections, cached: true %>
  </div>

  <div class="padding-3">
    <%= paginate @collections %>
  </div>
</div>
