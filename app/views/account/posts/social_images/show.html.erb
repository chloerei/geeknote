<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @post.title %></title>
    <style media="screen">
      body {
        padding: 0;
        margin: 0;
        color: #333;
      }

      .container {
        box-sizing: border-box;
        background: #cfe8fc;
        min-height: 100vh;
        min-width: 100vw;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 10vw;
      }

      .card {
        background: white;
        padding: 5vw;
        border-radius: 2vw;
        flex-grow: 1;
        display: flex;
        flex-direction: column;
      }

      .card__header {
        flex-grow: 1;
        min-height: 0;
      }

      .card__header h1 {
        margin: 1vw 0 5vw;
        font-size: 5vw;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
      }

      .card__footer {
        display: flex;
      }

      .avatar img {
        display: block;
        width: 5vw;
        height: 5vw;
        border-radius: 50%;
      }

      .author {
        font-size: 3vw;
        font-weight: bold;
      }

      .logo {
        font-size: 3vw;
        font-weight: bold;
        color: #777;
      }

      .logo img {
        display: block;
        height: 5vw;
      }

      .display-flex {
        display: flex;
      }

      .align-items-center {
        align-items: center;
      }

      .flex-grow-1 {
        flex-grow: 1;
      }

      .gap {
        gap: 1vw;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card">
        <div class="card__header">
          <h1 class="display-large"><%= @post.title.presence || t('common.untitled') %></h1>
        </div>
        <div class="display-flex align-items-center gap">
          <div class="flex-grow-1">
            <div class="display-flex align-items-center gap">
              <div class="avatar">
                <%= avatar_image_tag @post.user.avatar %>
              </div>

              <div class="author">
                <%= @post.user.name %>
              </div>
            </div>
          </div>
          <div class="logo">
            <% if @site.logo.attached? %>
              <%= image_tag @site.logo %>
            <% else %>
              <%= @site.name %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
