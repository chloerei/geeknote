<div class="container">
  <div class="layout-primary padding-3">
    <%= render 'account/dashboard/navbar' %>

    <div class="">
      <div class="breadcrumb headline-small padding-y-1 margin-bottom-3">
        <div class="breadcrumb__item">
          <a href="<%= account_dashboard_members_path(@account) %>">
            <%= t 'common.members' %>
          </a>
        </div>
        <div class="breadcrumb__item">
          <a href="<%= account_dashboard_member_path(@account, @member) %>">
            <%= @member.user ? @member.user.name : @member.invitation_email %>
          </a>
        </div>
        <div class="breadcrumb__item breadcrumb__item--active">
          <%= t 'common.edit' %>
        </div>
      </div>

      <div class="card card--elevated background-surface padding-3">
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>
