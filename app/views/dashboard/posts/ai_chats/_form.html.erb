<%# locals: (ai_chat:) %>

<%= form_with model: ai_chat, url: dashboard_post_ai_chats_path(@account.name, @post) do |form| %>
  <%= render "dashboard/posts/ai_chats/composer",
    form: form,
    field: :prompt,
    placeholder: t(".prompt_placeholder"),
    submit_label: t(".submit") %>
<% end %>
